<!DOCTYPE html>
<html>
  <head>
    <title>CRUD de Animal</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <meta charset="utf-8" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Preencha os dados abaixo e clique em Salvar o novo animal.</p>  
  <div>
    <form action="<%= action %>" method="POST">
      <table>
        <tr>
          <td>Nome</td>
          <td><input type="text" name="edtAnimalNome" value="<%= registro.aninome %>" /></td>
        </tr>
        <tr>
          <td>Idade</td>
          <td><input type="number" name="edtIdade" value="<%= registro.aniidade %>" /></td>
        </tr>
        <tr>
          <td>Cor</td>
          <td><input type="text" name="edtCor" value="<%= registro.anicor %>" /></td>
        </tr>
        <tr>
          <td>Gênero</td>
          <td><select name="cmbGenero">
                <option value="1" <% if(registro.gencodigo == 1) { %> selected <% } %> >Gato</option>
                <option value="2" <% if(registro.gencodigo == 2) { %> selected <% } %> >Cachorro</option>
                <option value="3" <% if(registro.gencodigo == 3) { %> selected <% } %> >Cavalo</option>
                <option value="4" <% if(registro.gencodigo == 4) { %> selected <% } %> >Papagaio</option>
                </select></td>
        </tr>
        <tr>
          <td>Descrição do animal</td>
          <td><input type="text" name="edtDescricao" value="<%= registro.anidescricao %>" /></td>
        </tr>
        <input  type="file" name="file" /> 
        <tr>
          <td><a href="/">Cancelar <button  name="file"  type="submit" value="Salvar" class="btn btn-primary">Save</button>   </a></td>
        </tr>
      </table>
        
        
    </form>
  </div>
  </body>
</html>

